## TypeScript implementations

This directory is reserved for TypeScript execution/generation backends for
Orin programs.

Authoring and analysis tools are kept under `/tooling/` (for example,
`/tooling/vscode-extension/`) so they remain separate from implementations.
